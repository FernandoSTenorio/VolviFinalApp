<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_addEvent"
        android:icon="@mipmap/volvi_ic_launcher_foreground"
        android:title="Add Post"
        app:showAsAction="always"/>

    <item android:id="@+id/action-calendar"
        android:title="Calendar"
        app:showAsAction="never"/>

    <item android:id="@+id/action-bloglis"
        android:title="Volvi Blog"
        app:showAsAction="never"/>

    <item android:id="@+id/action_signout"
        android:title="Signout"
        app:showAsAction="never"/>

</menu>